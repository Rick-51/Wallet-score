# Client

This folder contains the public Veritas wallet assessment experience.

- `page.tsx` implements manual wallet address entry, MetaMask connection, assessment submission, and status polling.
- `wallet-score-page.tsx` shows the connected wallet score, review refresh action, and verified transaction history.
- Public routes are `/` and `/my-wallet`.
